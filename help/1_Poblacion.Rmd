---
title: "Población"
output:
  html_document: default
---

#### Introducción

**Estás viendo la población simulada**. Al hacer un inventario, no conocemos $\mathcal{N}$ la población, no sabemos cuántos individuos hay ni cuáles son los valores de sus alturas y diámetros. Esto implica que en aplicaciones reales no conoceremos los valores de los parámetros de interés que querremos estimar. Sin embargo, las simulaciones son útiles para acercarnos al problema del muestreo en unas condiciones en las que sí que conocemos todos los individuos en el área a muestrear (cuadrado rojo), y podemos calcular todos los parámetros de interés para posteriormente analizar distintas configuraciones del muestreo.

#### Parámetros de interés (General)

A continuación, veremos tipos de parámetros de interés. Es importante que sepas diferenciar los tres tipos de parámetro que se presentan a continuación.

**Totales y totales por unidad de área**

De forma genérica, un total se define como la suma de una variable de interés $y_i$ para todos los árboles $i$ de la zona a muestrear. Un total por unidad de área simplemente consiste en dividir un total por el área muestreada $A$ que en general será conocida. Si usamos el símbolo $\mathcal{N}$ (N de bordes redondeados) para representar el número de árboles en la zona a muestrear (parámetro desconocido) tenemos que totales y totales por unidad de área se expresan como:

$$
Total(y)=\sum_{i=1}^{\mathcal{N}}y_i \qquad \qquad Total ~ por ~ unidad ~ área(y)=\frac{1}{A}\sum_{i=1}^{\mathcal{N}}y_i
$$

Dos ejemplos de totales por unidad de área son la densidad $N$ y el área basimétrica $G$. Para la densidad, $N$ (N con bordes rectos), $y_i$ es igual a 1 para todos los árboles, para $G$, $y_i$ es la sección normal (en metros cuadrados) de cada árbol.

**Ratios y funciones de totales**

Si bien los totales son, seguramente, los parámetros de interés más importantes desde el punto de los inventarios, muchas variables dasométricas que querremos estimar no son totales, pero sí que son funciones que implican a más de un total. En este sentido destacan los valores medios. Suma de la variable de interés para todos los individuos de la población dividido por el número de individuos, $\mathcal{N}$. Pero $\mathcal{N}$ es en sí un total donde $y_i$ es uno para todos los árboles, tenemos que los valores medios son ratios de totales o de totales por unidad de área ya que $\frac{1}{A}$ se cancela.

$$
Media(y)=\frac{\sum_{i=1}^{\mathcal{N}}y_i}{\mathcal{N}}=\frac{\frac{1}{A}\sum_{i=1}^{\mathcal{N}}y_i}{\frac{1}{A}\mathcal{N}}=\frac{\sum_{i=1}^{\mathcal{N}}y_i}{\sum_{i=1}^{\mathcal{N}}1}
$$

Otro ejemplo de parámetro de interés que se expresa como función de totales es el diámetro cuadrático medio $d_g$. Partiendo de la propia definición de $d_g$ o diámetro del árbol de sección normal media. Vemos que $d_g$ es una función de dos totales $G$ y $N$

$$
\frac{\pi d_g^2}{4}=\frac{AG}{AN} \rightarrow d_g = \sqrt{\frac{4G}{\pi N}}
$$

**Otros parámetros complejos**

Hay variables dasométricas que no son totales o funciones o ratios de totales. En este apartado destacan las alturas y diámetros dominantes, que suelen definirse como alturas o diámetros medios del conjunto de árboles dominantes.

$$
h_o= \frac{1}{\mathcal{N_{dominantes}}}\sum\limits_{i\in dominantes}h_i
$$

$$
d_o= \frac{1}{\mathcal{N_{dominantes}}}\sum\limits_{i\in dominantes}d_i
$$

Bajo el criterio de Assman, el conjunto de árboles dominantes es el conjunto de los 100 individuos con mayor diámetro de la hectárea. Estos parámetros, cuya definición es aparentemente sencilla, implican trabajar con un conjunto de árboles (el conjunto de los árboles dominantes) que es desconocido y la propia definición puede ser confusa. Por ejemplo, ¿Cómo establecemos la altura dominante de una masa de 1 ha de superficie donde hay 50 árboles?. En principio, no vamos a preocuparnos de estos detalles, pero es importante saber que existen y que en algún momento los encontraremos y tendremos que decidir como aproximarnos a dichos problemas.

#### Parámetros de interés (Ejemplos)

En este caso hemos considerado los siguientes parámetros de interés donde $A$ es el área conocida de la zona a muestrear:

**Totales**

-   Total de individuos

$$Total\:individuos= \sum_{i=1}^{\mathcal{N}}1$$

-   Total secciones normales

$$Total\:secciones\:normales(m²)=\sum_{i=1}^{\mathcal{N}}g_i=\sum_{i=1}^{\mathcal{N}}\frac{\pi}{4}dn_i^2$$

-   Total de volumen con corteza

$$Total\:volumen\:con\:corteza(m³)=\sum_{i=1}^{\mathcal{N}}vcc_i$$

**Totales por unidad de área**

-   Densidad (pies por ha) (Total por unidad de área)

$$N(individuos/ha)=\frac{1}{A}\sum_{i=1}^{\mathcal{N}}1$$

-   Área basimétrica (Total por unidad de área)

$$G(m²/ha)=\frac{1}{A}\sum_{i=1}^{\mathcal{N}}g_i=\sum_{i=1}^{\mathcal{N}}\frac{\pi}{4}dn_i^2$$ 

- Volumen con corteza por unidad de área (Volumen con corteza por ha)

$$VCC(m³/ha)=\frac{1}{A}\sum_{i=1}^{\mathcal{N}}vcc_i$$

**Ratios y funciones de totales o totales por unidad de área**

-   Diámetro cuadrático medio (función de totales)

$$dg(cm)=\sqrt{\frac{G}{N}\frac{4}{\pi}}=\sqrt{\frac{\sum_{i=1}^{\mathcal{N}}\frac{\pi}{4}dn_i^2}{\frac{1}{A}\sum_{i=1}^{\mathcal{N}}1}\frac{4}{\pi}}$$

-   Altura media (ratio de totales)

$$\bar{h}(m)=\frac{\sum_{i=1}^{\mathcal{N}}h_i}{\sum_{i=1}^{\mathcal{N}}1}$$

**Parámetros de dominancia**

-   Altura dominante (otros parámetros complejos)

$$h_o(m) =\sum_{i \in \mathcal{D}}^{\mathcal{N}}g_i=\sum_{i=1}^{\mathcal{N}}\frac{\pi}{4}dn_i² \:\: ,donde \: \mathcal{D} \text{ es el conjunto de árboles dominantes}$$

Bajo el criterio de Assman $\mathcal{D}$ es el conjunto de los 100 árboles más gruesos por hectárea
